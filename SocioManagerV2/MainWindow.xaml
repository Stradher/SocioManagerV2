<Window x:Class="SocioManagerV2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SocioManagerV2.ViewModels"
        Title="Socio Manager V2 - CRUD Operations"
        Height="700" Width="1400">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Socios List" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="➕ Add New Socio" 
                    Command="{Binding AddCommand}"
                    Background="#4CAF50" 
                    Foreground="White"
                    Padding="15,8"
                    FontWeight="Bold"
                    MinWidth="150"
                    Margin="0,0,10,0"/>

            <Button Content="🗑️ Delete Selected" 
                    Command="{Binding DeleteCommand}"
                    Background="#F44336" 
                    Foreground="White"
                    Padding="15,8"
                    FontWeight="Bold"
                    MinWidth="150"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Socios}"
                  SelectedItem="{Binding SelectedSocio, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Apellido1" Binding="{Binding Apellido1}" Width="*"/>
                <DataGridTextColumn Header="Apellido2" Binding="{Binding Apellido2}" Width="*"/>
                <DataGridTextColumn Header="DNI" Binding="{Binding Dni}" Width="100"/>
                <DataGridTextColumn Header="Número de Socio" Binding="{Binding NumeroDeSocio}" Width="120"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding NumeroDeTelefono}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
